{
  "version": "v1",
  "hooks": [
    {
      "name": "Fix Errors After Python Run",
      "trigger": "PostToolUse",
      "matcher": "execute_bash",
      "action": {
        "type": "agent",
        "prompt": "If a Python script just ran (command contains '.py' or 'python') and the output has errors (syntax, import, runtime, API), diagnose and fix the code, then re-run until clean. If the error reveals undocumented API behavior, note it for a docs update. Ignore non-Python commands."
      }
    }
  ]
}